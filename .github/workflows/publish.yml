name: Publish to pub.dev

on:
  push:
    tags:
      # Covers both v1.2.3 and v1.2.3+4 tags.
      - 'v*'

jobs:
  publish:
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
